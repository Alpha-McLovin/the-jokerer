package com.example.thejokerer.navigation

/**
 * Enumeration class representing different destinations in the application.
 *
 * The `Destinations` enum class defines possible destinations within the application,
 * such as the Home page and Favorites page. It's used for navigation purposes.
 */
enum class Destinations {
    Home,
    Favorites,
}
